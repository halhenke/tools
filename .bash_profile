echo "~/.bash_profile called!"
# Not sure that this file actually gets called under normal OS X shellbash setup...
# If not check ~/.bashrc and ~/.profile

#Most of my shell customization Macports etc is actually in .profile
source ~/.profile

